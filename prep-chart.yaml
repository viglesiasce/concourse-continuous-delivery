---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: devth/helm
    tag: 2.6.2

outputs:
- name: helm-repo

run:
  path: bash
  args:
  - -exc
  - |
    mkdir -p helm-repo
    helm package source/nginx helm-repo
    helm repo index --merge helm-repo
